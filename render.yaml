services:
  - type: web
    name: fake-news-detector
    env: python
    buildCommand: pip install gunicorn==21.2.0 && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free